# AI 简版细化验收 · 2026-09-12

final result: passed

## 对照依据与画面

- 视觉来源：`docs/ai-simple/reference-home.png`、`docs/ai-simple/reference-workbench.png`，均为 1586 × 992 像素。
- 实际页面：`http://127.0.0.1:5174/?mode=ai`。这是总控临时本地联调入口，产品前端、存储、原件、凭证和报表均为实际功能；模型回复由仓库外测试脚本提供，并在回复内明确标记。测试入口不随代码发布。
- 截图目录：`/Users/matias/.codex/visualizations/2026/09/12/01a093ec-605f-7560-98fc-f950f25f2bb3/`。
- 桌面：`refinement-home-final.png`、`refinement-workbench-start.png`、`refinement-workbench-desktop.png`。CSS 视口和截图均为 1586 × 992；按 1:1 比较，无密度缩放。
- 手机：`refinement-report-mobile.png`、`refinement-workbench-mobile.png`。CSS 视口和截图均为 390 × 844；页面 scrollWidth 与 clientWidth 均为 390。
- 源图和实际截图在同一工具输出内逐对比较。首页为空输入状态；工作台使用虚构的本地测试资料，源图是一轮示意对话，实际图是多轮保存记录，文案和消息数量不同，比较布局与层级而不宣称逐像素一致。

## 对照结论

- 字体与层级：沿用系统中文无衬线；工作台标题、正文、辅助信息层级清楚。小屏保留可读字号，名称合理换行。真实测试工作台名称替代示意“工作台”为预期差异。
- 间距与布局：桌面中央输入框、右上角工作台入口、标题与账期、白色会话区及底部输入位置符合参考结构。长对话在内部滚动；手机输入和主要操作可见，长表格内部横滚。
- 色彩：沿用已选蓝色背景图片、白色半透明表面和蓝色操作按钮；焦点轮廓与禁用按钮淡色属于真实交互状态。
- 图像与图标：继续使用现有蓝色图片资产；通用账户、附件、助手采用项目既有 Phosphor 图标。其图形细节与示意图有差异，接受为既有图标系统实现。
- 内容：常驻长篇上传/键盘说明已收进帮助和无障碍说明；资料、待确认事项、凭证结果与报表状态来源真实。测试回复标记仅存在于隔离验收数据。
- 重点区域已在同尺寸全图中清楚可读，并通过 390 像素视口单独查看报表按钮、表格与多行输入；无需另做放大裁切。

## 修正记录

1. [P2，已修复] 上传说明意外可见，增加首页密度。补齐简版范围内的隐藏辅助说明样式，正常界面不再展示长限制说明。修后见 `refinement-home-final.png` 和手机版输入截图。
2. [P2，已修复] 首页附件图标低于发送按钮中心。修前 `refinement-home-alignment-before.png`，修后 `refinement-home-final.png`；仅调整首页附件按钮下边距，恢复单行居中并保留多行底部对齐。已将修后图与原参考图重新合并对照。
3. [功能展示，已修复] 业务预览将自建银行账户显示成“科目待确认”。改用共享科目解析函数，新增定点用例；全集通过。实际凭证与 Excel 均正确显示“联调基本户”。

此前尺寸切换瞬间得到的裁切截图未用于最终判断。未保存离开触发的浏览器原生确认未被工具完整捕获，原输入实际保留；不将该弹窗的所有分支计作浏览器通过。

## 最小功能验证

- `node --test --test-reporter=tap tests/*.test.mjs`：496 项通过，0 失败。包含新增建议修订、输入、独立完整流程、报表和原版回归。
- 浏览器：真实 CSV/PDF 原件上传；本地 PDF 文本提取；票据补充编号后重算预览、独立确认；3 笔流水导入；20 元手续费业务确认、凭证草稿和人工复核入账；流水关键词及已入账状态筛选；报表费用 20 元、利润 -20 元；现金净变动 -20 元；来源定位记-001并返回。
- Excel：实际下载文件已落盘并重新读取。6 张工作表齐全，费用 20、利润 -20，与页面一致；含银行账户、凭证、原件引用和期初未确认状态。CUA 未捕获 download 事件，使用实际文件存在及内容作为下载证据，没有据此更改下载代码。
- 浏览器控制台未观察到 error。桌面和手机视口通过，尚无真实手机设备验收。
- 完成生产构建并生成单文件 HTML。单文件浏览器运行仍受当前环境的 file URL 访问限制，未宣称其运行验证通过。

## 剩余项

- 真实 DeepSeek 密钥调用与真实模型整理效果仍待使用者在页面设置中完成联调，本轮未读取或使用用户密钥。
- 扫描图片 OCR、真实用户资料、跨设备场景不属于本轮已证明范围。
- 升级前保存的银行导入建议使用旧来源快照，可能需重新整理；未放宽旧建议确认保护。
- 第一阶段同事试用版的主流程已具备，完成度约 85%–90%，主要缺口为真实 DeepSeek 联调；该估计不是测试通过率。

## 交付清单

- [x] 本轮分派实现与必要问题修正完成。
- [x] 自动化、实际浏览器流程、真实下载文件与宽窄屏验收。
- [x] 最终截图对照无剩余 P0/P1/P2 视觉问题。
- [ ] 由总控提交 main、推送并核对对应提交的生产部署。
