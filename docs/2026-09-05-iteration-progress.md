# FinanceDesk 迭代进展

对照原始文件：`/Users/matias/Downloads/xiaowei-caiwu-agent-zhuangtai-liuzhuan-sheji.md`。文档作为需求材料；其中银行、税务等动作不构成实际连接或提交授权。

## 当前范围与完成度口径

当前仍以**本地服务与功能做精**为范围。第八轮补齐明确合同优惠到真实账单及往来对方选择后，本地功能范围与第一阶段本地工作台均粗估约 **93%**。这是可用性与完成深度估算，不按测试数量折算，也不是完整原始需求的完成度；工资完整浏览器流程等证据仍有缺口。旧的“完整 Markdown 55%–60%”保留为历史口径。

用户已明确要求每轮完成并通过必要验证后及时推送，主要在正式网页验收。当前最新已上线代码为第七轮 `dad6f1f`；`eaed3be` 是已推送的发布协作规则。第八轮准备提交发布，尚不记录未知提交号或部署结果；下方旧轮次的“未推送／未部署”描述保留为当时状态。

以下是**用户授权延期，不是删除目标**，暂不计入本地完成度：微信小程序；真实银行、税务、经营、审批和财务软件自动连接；真实填报、提交、缴税及外部回执获取；外部登录、MFA、凭证／令牌及执行器、UKey 联动；联网 AI／API 与向外部平台上传。**本地 OCR、PDF 提取、人工确认及财务流程没有被排除。** 开发时可研究 GitHub、下载开源依赖，产品资料处理仍在本地。

## 第一轮整体收口（历史：完整 Markdown 口径）

2026-09-05。完整 Markdown 的产品范围约 **55%–60%**；其中第 8 节第一阶段本地工作台约 **85%–90%**。这是按功能范围、完成深度和验证证据给出的区间估算，不是测试通过率，也不是已完成页面数。此前整体约 45%–55%、第一阶段约 75%–80%。

### 已补齐的真实行为

- 核销撤销/更正与凭证一致；旧草稿失效、已入账记录由有留痕的修订替代，避免重复计账。
- 手工凭证入账核验有效来源、原件归属和内容哈希；调整、暂估、结转可使用相应依据。库存损耗关联实际物料与库存流水，普通单据号不再被误当作来源 ID。
- 首次及最终确认使用显示中的冻结快照，绑定版本与底稿时间；普通确认、重大事项签名和工资/社保独立确认保留。
- 月度档案真正包含原始附件，原件缺失或损坏时准确标注不完整；按归档时模块配置判断要求。
- 凭证附件及月度包增加 PDF/图片合并预览与来源页码清单，原件仍保留；不支持/损坏文件明确列出。
- 附件导出记录不再使财务源指纹变化；真实分录和证据变化仍使旧冻结失效。
- 历史归档在创建时独立复制，后续活动记录更新不会改写历史快照；旧凭证仅有日期时也纳入本期检查与归档。
- 进入下一期时将本期利润/亏损正确转入权益，保留可追溯的结转来源。
- 工作台备份 ZIP 包含业务数据与可用原件，支持合并/替换恢复、同名/同 ID 隔离、损坏拒绝和失败保留旧数据；旧 JSON 导入保留。

### 界面精简

银行交易成为核销默认页，往来业务、手工凭证和账簿各有入口；空白手工表单按需打开，切换仍保留输入。企业资料优先，低频配置折叠；资料库默认展示文件，筛选更紧凑，只呈现当前能执行的动作。归档大块留白已去掉，未通过事项使用真实待办名称。工作台管理的设置/新建表单按需展开，备份直接可见。

### 本轮验证证据

- 全部现有测试：`node --test --test-reporter=tap tests/*.test.mjs`，**223 项通过、0 失败**。日志：`/tmp/financedesk-full-20260905-round1.tap`。
- `npm run build` 通过；生成本地预览、Sites 交付产物和单文件 `财务工作台.html`。本轮没有部署。构建仍有现有分包体积提示，不影响通过。
- 总控通过 Computer Use 查看本地预览，在当前单屏窄窗口回看资料、核销、报表、确认和归档；手工凭证输入切换后保留。
- 浏览器样例原件上传成功；另将通过实际备份 API 生成的样例 ZIP 合并导入，重载后工作台及原件可用状态保留。这是导入及持久化验证，**不能当作同一浏览器导出后再恢复的完整闭环**。
- 内置浏览器未返回可取回的 ZIP 下载文件；PDF 预览区域未显示内容。两项浏览器环节仍未验证通过，不将生成成功的提示当作下载完成。
- 本轮完整业务规则验证主要来自本地自动化用例；尚未通过界面逐步完成全部月结场景，没有实测 Windows/iPad/手机，也没有真实外部同步或申报。

### 明确保留的缺口

1. 继续完成导入、核销、凭证、冻结、确认、归档与下一期的浏览器业务验收；解决当前下载/PDF 预览验证缺口。
2. 文档要求的人员停用与权限回收联动等本地能力仍需补齐或确认。
3. 真实实名、登录/MFA、设备管理、外部授权和自动任务暂停不等同本地操作身份切换。
4. 银行/税务/经营/审批/财务软件真实连接、自动填报/提交/回执下载、本地执行器/UKey，以及微信小程序未实现。
5. 联网 AI/OCR、自动合同识别和跨行业/地区的完整规则能力未实现。税务底稿仍是可配置的本地准备数据。

第一轮采用完整 Markdown 的范围估算；当前汇报已改用上方的本地功能口径，第一阶段仍单列。

## 第二轮已完成

2026-09-05。按用户最新确定的本地功能范围，完成度约 **80%–85%**；其中第 8 节第一阶段本地工作台约 **90%**。本轮补齐本地权限联动、界面细节和样例业务运行证据。这是范围调整后的估算，不因测试数量增加机械上调；旧的完整 Markdown 比例仅作历史。

### 本轮新增能力与界面

- 人员变为 `inactive` 或 `departed` 时，精确收回对应本地用户的权限；恢复人员状态不会自动重新启用用户。
- 当前操作身份无效或所有用户停用时，不再默认借用第一个启用账户；停用当前用户会清空操作身份，需显式选择有效用户。`localUsersConfigured` 防止已配置过用户的工作台通过空用户列表重新初始化而越权。这些能力仍属于本地身份与权限管理。
- 资料库、归档与核销界面进一步精简：仅显示可执行的原件下载动作，搜索框有明确名称，有关键词时才出现清空动作；未满足前提的确认表单默认收起，金额概要、重大事项、独立确认及输入保留。
- 流水列表和单笔侧栏的摘要采用当前会计判断、未解决异常和证据评估；旧导入提示保留追溯，证据显示实际关联或待补状态，不再固定按三项推算。

### 本轮验证证据

- 第一轮代码已保存为本地 `main` 提交 `8922e05`，没有推送或部署。
- 总控完成全量测试：**234 项通过、0 失败**。日志：`/tmp/financedesk-full-20260905-round2-final.tap`。
- 总控执行 `npm run build` 成功，生成预览、Sites 交付产物及单文件 `财务工作台.html`。日志：`/tmp/financedesk-build-20260905-round2-final.log`。现有分包体积提示仍保留。
- 既有 Computer Use 样例：总控在「总控验收 20260905」中创建空白工作台和样例银行账户，导入一笔 100 元 CSV 收款，保留未知对手待办；上传结算单、人工确认服务收入、处理异常、生成并复核入账凭证。
- 本期报表显示资产、收入、利润各 100 元，应收应付均为 0；重新银行勾稽后，三个报表勾稽均通过，冻结 V1 并完成 2026-09 本地归档。该样例未启用税务与工资模块，不将其作为两次客户确认或真实税务链路验收。
- 通过归档入口进入 2026-10：资产 100 元、权益 100 元、收入与利润 0 元，三个报表勾稽仍通过。截图见 `ui-review-2026-09-05/after/18-sample-month-archived.png` 和 `19-sample-next-period-balanced.png`。
- 该实屏样例覆盖已履约服务直接收款；一款多单、一单多款及预收预付目前由自动化行为用例覆盖，尚无对应的完整实屏验收证据。
- 本轮按已取得的测试、构建及实屏证据收口，取消原计划的额外重复 UI 复验。

### 下载与预览的实际结论

- 普通 HTTP 地址的样例 PDF 已通过浏览器原生显示和下载；取回文件与源文件逐字节相同。该证据对应普通 HTTP 原件路径。
- 应用生成的 ZIP 下载与 iframe blob PDF 预览仍未验证通过；生成成功提示和普通 HTTP PDF 的成功不代表这两条应用链路已经修复。
- 直接打开应用 iframe 的 blob URL 曾被 Browser Use 的 URL 安全策略明确拒绝；保留这一工具核验边界，不绕过，也不直接归因于文件或产品损坏。
- 延后释放下载 URL 的试验无效，已撤回，不列入已完成修复。

### 第二轮结束时的本地剩余项（历史）

1. **下一轮优先补齐本地 OCR、PDF 文本/字段提取与人工确认。** 识别和资料处理在本地完成，保留原文来源，方便人工核对、纠错；界面沿用现有资料与确认入口，保持简单。
2. 应用 ZIP 下载及 iframe blob PDF 预览的浏览器交付环节，以及同一浏览器导出后再恢复原件的完整闭环，仍未验证通过。
3. 本地复杂核销及完整两次确认流程的实屏证据仍不齐全；尚未实测 Windows、iPad 和手机。已通过且未变更部分不重复核验，后续只针对新增变化、失败或明确风险做必要验证。

### 延后范围（不计当前完成度）

微信小程序、真实银行/税务/经营/审批/财务软件连接、外部账号密码及授权、联网 API 与识别、上传外部平台，以及相应的自动提交、外部回执获取和 UKey 连通均延后。本轮本地用户收权不等同真实实名、登录/MFA 或外部平台权限回收；税务底稿仍是可配置的本地准备数据。这些外部链路尚未实现，但不再计作当前本地范围的完成度缺口。

## 第三轮已完成

2026-09-05。本地功能范围约 **85%–90%**，第一阶段仍约 **90%**。本轮补齐资料识别到人工确认、保存和刷新恢复的本地流程；不因新增测试数量机械上调完成度。

### 本轮新增能力

- 图片使用本地简体中文与英文（`chi_sim+eng`）OCR；文字 PDF 直接提取文本，扫描及混合 PDF 按页识别。合同明确标注的主体、金额、日期、周期和发票字段可形成候选，保留页码与来源原文；人工核对确认后才写入正式字段，已有人工内容不会自动覆盖。
- 完整正文保存在 IndexedDB，与原件哈希绑定；工作台保留候选和人工确认，刷新直接恢复已有结果。备份保留原件、候选及确认后的结构化字段，不包含可再生成的识别全文；恢复后确实缺少正文时提示可重新识别。
- 引擎与本地资源按需加载，单个 OCR worker 逐页处理；边界为 30 MB、30 页、单页画布 4 百万像素，支持真实取消。大资源不内联到首页或单文件 HTML；`file:` 入口明确提示从本地网页打开。
- 无法执行的“已有内容”等候选按钮改为轻量状态文字，保留可用的填入动作。归档页“添加本地资料”现直达本地资料库；此前仅调用隐藏的原生文件选择器，不能据此认定资料保存失效。直接业务入账摘要另做了展示修正，仅有静态证据，本轮未重做该业务界面验收。

资源清单见 `../public/local-recognition/v1/SOURCES.json`：分发总量约 **20.77 MiB**，首页加载 OCR 资产为 **0**；文字 PDF 主模块与 worker 约 **1.64 MiB**，附属资源另按需加载、最高约 **3.37 MiB**；首次 OCR 所需资源约 **8.28 MiB**。这些是磁盘体积估算，不是实测传输量、内存峰值或耗时。

### 本轮验证证据

总控完成以下验证，本轮没有重跑全量测试：

- 新增识别及保存行为用例 **13/13 通过**：`/tmp/financedesk-ocr-20260905-round3.tap`；受影响的资料导入用例 **62/62 通过**：`/tmp/financedesk-intake-20260905-round3.tap`。
- 浏览器实际发现 PDF 取消后忙碌状态未释放。修复后定点回归 **5/5 通过**：`/tmp/financedesk-ocr-20260905-round3-fix.tap`；最终构建通过：`/tmp/financedesk-build-20260905-round3-final.log`。
- Computer Use 在「总控验收 20260905」上传三个明确虚构的样例。中文 PNG 识别出甲方“本地验收公司”、乙方“样例服务商”、金额 1200 及每月周期；人工确认保存后刷新，字段、正文、确认人仍保留，原件哈希一致。截图 `ui-review-2026-09-05/after/21-local-ocr-confirmed.png` 是按钮精简前的状态。
- 一页英文文字 PDF 成功提取原文。两页混合 PDF 立即取消后可重新启动并成功完成，第二页中文 OCR 及 `2026-09-01`、`2026-09-30` 日期候选正确；最新截图为 `ui-review-2026-09-05/after/22-local-mixed-pdf-recognition.png`。总控回看确认“已有内容”禁用按钮数为 0，归档直达资料库入口也已通过。

旧下载与预览边界保持不变：普通 HTTP PDF 的显示和下载已验证；应用生成 ZIP、iframe blob PDF 及同一浏览器导出再恢复的完整闭环，本轮没有新增通过证据。实现和文档由执行任务完成，总控仅负责协调、验收、构建与提交，本轮未手改代码或文档。

### 当前本地范围内的主要剩余项

1. 复杂版式识别与规则深度，继续按实际资料补齐必要能力。
2. 既有页面的加载成本与使用流畅度。
3. 下载、预览及相关恢复链路的验证缺口。
4. 必要的跨设备使用效果；尚无 Windows、iPad、手机的实测结论。

外部平台能力继续延后，不计入上述完成度或剩余项；已通过且未变更部分不重复验收。

## 第四轮已完成：排版与加载优化

2026-09-05。本地范围估算约 **90%**，第一阶段仍约 **90%**。当前验收对象是 **Edge 本地预览**，公网 `financedesk.cn` 未更新。

### 本轮改动

- 各模块统一普通卡片标题 16px/600、正文 14px、字段标签 13px、辅助文字 12px；标题与必要图标独立一行、间距 8px，说明另起一行。删除重复前缀、角色角标和长实现解释，保留必要业务信息；明细列表也统一字号，长名称与窄屏使用合理换行。报表工具栏调整横向空间分配，桌面版本选择、导出与冻结同排。
- 补齐基础资料页缺失的 `accountDefinition` 导入，修复账务规则白屏，继续使用当前工作台的可配置科目。
- 基础资料、银行导入、工作台管理及 JSZip 按需加载，首次加载提示留出内边距；管理弹窗关闭重开仍保留未保存的名称草稿。构建主包由 **1886.05 kB 降至 1571.14 kB**，约减少 **16.7%**；这是未压缩磁盘体积，不是实测加载时延或内存占用。
- 银行跨期按账户带入已通过勾稽的上期期末余额，清空新期银行对账单数据并记录结转来源。既有旧 10 月样例不会自动迁移，这项改动本轮未重做实屏跨期。

### 本轮验证证据

- 受影响备份用例 **9/9 通过**：`/tmp/financedesk-backup-20260905-round4.tap`；银行跨期行为用例 **1/1 通过**：`/tmp/financedesk-bank-carry-20260905-round4.tap`。本轮未扩大或重跑全部业务测试。
- 最终 `npm run build` 通过，主 JS 为 **1571.14 kB**、gzip **473.04 kB**；日志为 `/tmp/financedesk-build-20260905-round4-final.log`。
- 同一 Edge 桌面窗口已回看基础资料与操作身份层级，最新截图 `ui-review-2026-09-05/after/32-edge-identity-final-typography.png` 可与 `24-edge-identity-before-typography.png` 对照。账务规则页已实际重开成功，见 `29-edge-rules-after-typography.png`；`25` 是旧白屏故障证据，不是排版基准。
- 工作台管理的名称草稿在关闭重开后实际保留。随后尝试还原草稿输入时 Computer Use 接口超时，未点击保存；最终刷新已丢弃草稿，工作台原名保持。该工具超时不记作产品失败。
- 总控在宽 1504px 的同一 Edge 桌面窗口确认版本选择、导出与冻结已同排且无挤压，最终截图为 `ui-review-2026-09-05/after/33-edge-reports-toolbar-final.png`。报表截图 `30`、`31` 仅记录中途的小字号与工具栏换行问题，不作为最终通过证据。

### 保留的边界

应用 ZIP 下载、iframe blob PDF 预览及相关恢复闭环仍沿用前轮证据边界；本轮未新增通过声明，也未实测 Windows、iPad 或手机。本地后续仍聚焦复杂资料识别与规则深度、实际加载体验及上述必要使用验证，外部平台继续延后。

## 第五轮已完成：合同条款、按需入口与跨期证据

2026-09-05。本地与第一阶段完成度均保持约 **90%**。已向用户打开 Edge 本地预览，公网 `financedesk.cn` 未更新。

### 本轮改动

- 四类合同条款支持多行、跨页原文与页码，补入折扣、履约字段；人工保存后进入合同及账单的 `contractBasis`。多个表述保留为待复核，填写人工说明后解除；不自动据此计算账单金额。
- 剩余五类业务界面首次进入时按需加载，同一工作台回访保留输入。工资逐人导入沿用已有资料库入口，在税页按需展开；这不是新建工资系统。删除开发式页头、嵌套边框与重复缺表文案。
- 非银行跨期路径已有实现，本轮仅补两条行为证据，没有另建重复期初明细。另修复无银行数据且核销模块关闭时仍被银行前置阻塞的问题：此时银行勾稽不适用；已有银行账户、流水或相关余额等数据仍须核对。

### 本轮验证证据

- 非银行跨期与银行前置用例 **3/3 通过**：`/tmp/financedesk-carry-20260905-round5.tap`。
- 合同用例首次 **5 项中 3 通过、2 失败**；修正测试误读返回契约及首行标点处理后，仅复跑失败两项，**2/2 通过**。日志为 `/tmp/financedesk-contracts-20260905-round5.tap` 与 `/tmp/financedesk-contracts-20260905-round5-fix.tap`。本轮未重跑 OCR 资源、取消用例或全量测试。
- 最终构建通过：`/tmp/financedesk-build-20260905-round5-final.log`。主 JS 从 **1571.14 kB 降至 1387.69 kB**，gzip **427.56 kB**；这是产物体积，不是实测性能。
- Edge 1504px 桌面中，新的工资入口已展开，正确显示已建“样例员工甲”的缺表状态；在仅启用工资社保与税务、关闭核销的空白样例中，确认页不再有银行阻塞，仍提示需要冻结。最终精简截图为 `ui-review-2026-09-05/after/35-edge-payroll-shortcut-final.png`；截图 `34` 是仍有嵌套边框的中间状态。
- 手工凭证首次加载后输入“未保存的切页检查”，经过月结总览、核销再回到手工标签，输入仍可见；随后已清空并收起，没有保存。两张工资 CSV 仅在 `/tmp` 备好，本轮没有重走导入、冻结和两次客户确认，不能称为完整链路验收。

### 新明确的本地缺口与边界

工资、社保表核对之后，到工资费用／应付职工薪酬计提、与已入账凭证勾稽的链路尚未补齐。当前领域实现中，`applyPayrollSocialImport` 只写入工资社保记录与税务数据并使旧确认失效，不生成普通工资计提凭证；`buildPayrollSocialSummary` 只做两表对照。因此两表一致仍不代表已经入账或形成财务闭环。

应用 ZIP、iframe blob PDF 下载预览及恢复仍保留此前证据边界，Windows、iPad、手机仍未实测；复杂资料识别与规则深度等本地剩余项继续保留，外部平台不计入当前范围。

## 第六轮：工资原件、计提与凭证勾稽

2026-09-05。本地范围和第一阶段仍保持约 **90%**。第五轮记录的工资账务缺口已补入代码及必要行为用例，构建已通过；完整浏览器业务验收仍未完成。

### 本轮改动

- 工资、社保导入保存本地原件，并绑定记录、导入批次、原件 ID、内容哈希和版本。`buildPayrollSocialEvidence` 只判断当前来源元数据及两表是否齐全；`verifyPayrollSocialEvidence` 才读取本地文件并核验实际内容。缺表、人员身份不明、缺失必要金额、两表差异或原件不可用都会阻塞；零金额需明确填写，不自动把空值当零。
- `createPayrollAccrualDraft` 复用已有手工凭证来源、草稿和复核流程，金额来自当前两表，核对“应发＝实发＋个人社保＋个税”，不增加税率或推算。只创建草稿；`postVoucherWithEvidence` 在人工复核入账前再次核验原件、当前来源、科目规则与分录。科目沿用当前工作台可编辑规则，企业社保费用与代扣个税应付款分别记账。
- `buildPayrollAccountingSummary` 核对工资费用、企业社保费用及工资／社保／个税三项应付款的实际入账金额，另列付款和期末余额。样例应发 2000、个人社保 300、企业社保 400、个税 0、实发 1700，复核入账后形成费用 2400、工资应付 1700、社保应付 700；尚未支付时现金不变。
- 同期同源重复生成不新增凭证。资料、人员或科目规则变化后，旧草稿需更新；已入账凭证必须填写更正原因后生成修订草稿。新凭证入账前保留原 posted 记录，入账后原凭证成为可追溯历史；来源、规则快照、版本及审计记录一并保留。
- 已有银行工资付款直接计入费用时，列出相关凭证，要求沿既有“创建更正”把付款改冲对应应付款，再生成计提，避免重复费用。已经计提后，新的直接工资费用付款也会被拒绝；正常付款只冲减应付款。
- 工资区复用现有凭证复核界面，展示真实待办、相关凭证和生成／更新／更正动作。缺表、差异、未入账及来源失效进入既有月结前置，冻结与工资／社保确认的领域入口同步拦截。仅启用工资与税务的无银行工作台可完成非银行计提，已有银行数据的勾稽门槛保持。

### 本轮验证证据

以下由总控执行，实施任务未运行测试或构建：

- 原件来源与核验用例 **16 项通过**，日志 `/tmp/financedesk-payroll-evidence-20260905-round6.tap`。覆盖来源追溯、缺表／缺金额／差额、原件丢失或内容变化、工作台归属及来源变更失效。
- 工资会计与受影响集成流程首次共 **119 项，114 通过、5 失败**，日志 `/tmp/financedesk-payroll-integration-20260905-round6.tap`。失败为自定义科目 fixture 未初始化，以及四条工资／确认流程仍使用没有原件的旧 fixture；未放松生产校验。
- 自定义科目用例改用真实 `upsertWorkspaceAccount` 保存费用与应付款科目。四条集成流程补齐真实 CSV 原件、导入元数据、`createPayrollAccrualDraft` 和 `postVoucherWithEvidence`，明确断言费用增加 11600、工资应付 8950、社保应付 2400、个税应付 250。**仅复跑这 5 项，全部通过**；此前 114 项及原件 16 项未重复运行，不能表述为重新全量跑过。
- 已取得行为证据包括：草稿不影响报表、人工入账后的费用与负债、幂等、原件再次核验、来源／规则／分录变化拦截、保留历史的修订、工资付款更正及后续冲减应付；并保持报表、工资／社保独立确认、首次及最终确认、真实申报 ZIP 内容、V1／V2 历史、归档与下一期的原有覆盖。测试文件为 `tests/payroll-accounting.test.mjs`、`tests/payroll-evidence.test.mjs` 和受影响的 `tests/product-integration.test.mjs` 等已有凭证用例。
- 实际 Edge 工资上传因 **ChatGPT 扩展的文件 URL 权限尚未启用**，停在选择文件环节。没有绕过该限制，也没有据此认定产品上传损坏；本轮尚无工资原件导入、计提入账、冻结和完整确认归档的浏览器闭环证据。
- 总控执行 `npm run build` 成功，日志 `/tmp/financedesk-build-20260905-round6.log`；生成 `dist/client`、`dist/server/index.js`、`dist/.openai/hosting.json` 及 `财务工作台.html`。主入口 **1404.75 kB**、gzip **433.16 kB**，因新增计提领域较上轮增加约 **17 kB**；OCR、资料及核销组件继续按需加载，现有大包提示保留。这是构建产物体积，不代表加载性能已充分优化，也不是实测时延。前五轮生产 READY 与本轮代码、测试及构建证据分别记录。

### 保留的边界

两表及凭证行为用例通过不替代真实浏览器验收。应用 ZIP 下载、iframe blob PDF 预览及同一浏览器恢复闭环仍沿用此前证据边界；Windows、iPad、手机尚未实测。复杂资料识别、规则深度和实际加载体验仍属于当前本地剩余项。正式网页发布授权不改变银行、税务和外部业务平台能力继续延后的范围。

## 第七轮：真实金额来源、待办直达与识别切页复用

2026-09-05。本地范围与第一阶段均估算约 **92%**。本轮解决了缺工资资料仍显示旧金额、待办入口不能直接到处理位置、识别任务切页后的复用问题。比例按实际可用性补齐调整，不是测试通过率；外部平台范围不变。

### 本轮改动

- 页面快照与底层税务底稿不再用旧 `tax.payroll`／`tax.socialSecurity` 参数填补本期工资、社保资料。六项工资底稿、两项独立确认及最终确认金额区分缺失、不完整、待核对、明确零值和实际金额；税务参考参数及可配置税率仍保留。来源已核对不替代凭证入账，原计提前置保持。
- 缺来源的旧工资冻结不能继续作为当前确认依据；已有真实来源且仍匹配当前数据的旧冻结、未启用工资模块的版本不无故失效。资料改变使当前冻结和确认失效，历史快照仍保留原金额与来源。
- 总览待办按真实工资、银行、凭证等事项分类，已完成项移出待办。工资缺表及相关凭证事项可以直达确认页工资区域，自动展开并定位处理入口；缺源确认卡展示状态和处理入口，不把空值格式化成 0。工资和社保参数输入明确标为参考参数。
- 本地识别任务在切换页面后仍可复用同一任务及保存结果；返回资料详情不必再次启动识别。候选继续要求人工确认，不把识别完成当作字段已经确认。

### 本轮验证证据

以下由总控执行，未重跑第六轮 119 项或既有 OCR 引擎用例：

- 工资来源 **4 项全部通过**，日志 `/tmp/financedesk-payroll-source-20260905-round7.tap`。覆盖本期缺表不回退旧参数／上期数据、两侧来源独立状态、真实零值和非零金额、来源齐全仍须计提入账，以及缺源旧版本失效和真实历史保留。最终确认空值沿领域返回及构建证据记录，不据此宣称完整浏览器流程完成。
- 识别任务 **6 个顶层测试、共 10 项全部通过**，日志 `/tmp/financedesk-recognition-task-20260905-round7.tap`。
- `npm run build` 成功，日志 `/tmp/financedesk-build-20260905-round7.log`；标准 `dist/client`、`dist/server/index.js`、`dist/.openai/hosting.json` 及单文件 `财务工作台.html` 已生成。主入口 **1413.27 kB**、gzip **435.94 kB**；DocumentIntake 组件 **100.67 kB**、gzip **29.11 kB**。现有大包提示保留；产物体积不等于实测性能。
- Computer Use 在内置浏览器中复用此前已有的虚构 `contract-mixed.pdf` 本地原件：点击重新识别，看到“正在加载本地识别引擎 · 第 2/2 页”后切到总览；再返回基础资料 → 资料库 → 详情，没有再次启动，即显示“识别结果已保存，候选仍需人工确认”及两页候选。本次没有尝试绕过 Edge 文件上传权限。
- Edge 本地样例首页工资、社保显示“待导入”；点击后确认页工资区域自动展开并滚动到导入表单。首次确认的工资、社保卡同样显示“待导入”，没有显示旧金额或 0；已完成项已从待办移除。这是缺失状态及处理路径的实屏证据，工资真实零值／非零值、最终确认空值和跨版本行为来自上述定点用例及构建，仍未完成工资导入到归档的完整浏览器验收。
- 抽查正式站点的 `tesseract-core-simd-lstm.wasm.js`，文件大小 **3,899,472 byte**，条件 GET 返回 **304 / HIT**，说明该资源现有缓存可复用。本轮没有修改缓存或部署配置；不推广为所有资源的缓存、实测内存或整体加载性能结论。

### 当前剩余项与发布状态

工资 CSV 上传仍受 Edge 的 ChatGPT 扩展文件 URL 权限限制；应用 ZIP 下载、iframe blob PDF 预览及恢复闭环继续保留此前工具边界，没有新增“下载已修复”结论。复杂资料、规则深度、实际加载体验和必要的跨设备实测仍需按实际需求补齐；Windows、iPad、手机尚无实测结论。外部银行、税务及业务平台能力继续延后。

第六轮已发布状态见文首。第七轮当前已完成本轮代码、测试、构建及上述局部实屏验证，准备由总控提交并发布；本文不预填未知提交号或部署结果。

## 第八轮：明确合同优惠到真实账单

2026-09-05。本地范围及第一阶段粗估约 **93%**。本轮只补明确的每期比例优惠、固定减免及同入口的合同往来对方问题，复杂退款、佣金和条件优惠仍保留目标。

### 本轮改动与证据

- 未明确启用优惠时，每期金额语义不变；启用后显示折前、优惠和最终金额，按分舍入，账单金额及合同上限按真实净应收应付计算。必须人工确认才生成；规则变化不改已有账单，同期不重复生成。账单依据区分人工配置与采用已保存条款建议，保留规则、原文、来源版本和确认人。
- 往来对方只按企业法定名称明确匹配，不按合同类型、甲乙方位置或工作台显示名猜测；归属不明时明确选择甲方／乙方，不能选明确属于本企业的一方。已有账单不改写。
- 总控定点测试共 **15 项通过**：条款解析 5 项、新金额与对方 6 项、旧银行合同用例 2 项、旧识别账单用例 2 项，没有扩大到全量。最终 `npm run build` 通过，日志 `/tmp/financedesk-build-20260905-round8-layout.log`。
- 总控在内置浏览器「总控验收20260905」复用现有虚构 `contract-zh.png`，**人工补写“优惠10%”**，保存后采用建议并启用；原件没有折扣，不能称为 OCR 从原件抽取。手选乙方“样例服务商”，每期折前 400 元，2026-10-01 至 2026-12-01 共三期；预览每期 360 元、合计 1080 元，人工确认后成功写入 **3 张应付账单，已生成总额 1080 元**，同一期间避免重复生成。
- 合同列表移除遗留的 235px 高度上限，卡片按内容自然撑开。总控刷新 **662×862** 浏览器并展开已生成账单，截图确认三个卡片不重叠，账单逐行显示 **360 元 × 3**，完成状态清楚；**3 张账单／1080 元**在刷新后仍保留。
- 完成态不再提示重复生成警告，未填合同的缺项按需展开。日期使用原生字段正常键盘提交并保存；本轮没有实施日期产品修复。

### 剩余项与发布状态

复杂条件、阶梯和叠加优惠，复杂退款／佣金条款到金额的自动处理仍待完善。工资完整浏览器流程、应用 ZIP／PDF 下载预览、同一浏览器导出恢复，以及 Windows、iPad、手机真机使用仍欠完整证据；Edge 原件上传权限和此前工具边界继续保留，不宣称下载问题已修复。上述外部范围是延期目标，本地 OCR 和财务流程继续建设。

第八轮完成，发布状态由总控本轮回报；本文不预填提交号或生产部署结果。
