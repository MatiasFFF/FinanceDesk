# 2026-09-09 本地流程修复矩阵

本轮按已冻结七项实施。应用服务可供可信宿主复用，不等于已接模型、外部银行或税务平台。实施任务只写源代码、必要测试和本文档；测试、构建、浏览器验收、提交发布由总控执行并汇报。

| 已查实问题 | 本轮处理及页面接入 | 定点证据/永久回归 |
| --- | --- | --- |
| 9 月库存 10 件/200 元，切 10 月显示 0 | 统一分仓跨月承接；未归档回溯逐月重估；归档值原样展示；去复核使用已有 manualVouchers 导航 | 新 dist 浏览器实测：10 月承接 10 件/200 元，领用 1 件后为 9 件/180 元，刷新保留；回到 9 月仍为 10 件/200 元。“去手工凭证复核”进入正确页面。inventory-ledger 多仓、空月、晚新增、回溯/归档、舍入用例通过 |
| 9 月通用表单能改 8 月归档账单 | 共享账单写规则覆盖保存、状态、删除、红票；关键财务关系保护，备注/合法延长期限保留 | 新 dist 基础表单实测：8 月归档账单 1000 改 500 被拒绝并保留 1000；本期未使用账单 200 改 250 成功。store 保存/状态/删除入口及 bill-write-rules 用例通过 |
| 普通草稿无取消入口，补件任务一直阻塞 | 手工和通用列表接 cancelVoucherDraft；只解决自身任务，保留取消历史和更正原记录 | 浏览器取消误建草稿并保留历史；定点确认自身任务 1→0、pendingVouchers=0。voucher-draft-cancel 用例通过 |
| 原件核验后以整份旧工作台覆盖，或拒绝无关修改 | 五条入账 UI 共用 postWorkspaceVoucher；核验后读取最新目标，同步领域入账+保存；对实际源冲突拒绝/重新校验 | 浏览器新建调整凭证，上传计算文件、借贷各 20 元、保存草稿，再核验原件并完成首张凭证入账。voucher-post-service 并发保留/相关来源与身份变化、reconciliation-engine 服务层更正原子性用例通过；未将一次页面操作当作所有入账链路的浏览器验收 |
| 回执 await 期间生成新包后被错误改绑；底层可传空原件 | importWorkspaceReceipt 固定目标包及原件；底层 attachReceipt 也校验；App 保留原离开保护 | 浏览器从已冻结、确认、导包的独立验收账本上传测试回执，核对固定包 hash/version，刷新后下载原件，bytes cmp 一致，再完成本期归档。filing-file-service 后台月、错包、空原件、同月换包、真实业务引用保留及核验期间原件删除用例通过 |
| Excel/申报包内容生成被 DOM 下载绑定 | 提取 generateFrozenReportExcel / generateLocalFilingPackage；浏览器导出复用生成结果 | 无 document/window 的 Node 生成并读回 7 张 Excel sheet 与 11 项 ZIP 内容；内容/过期版本用例通过。浏览器从归档报表页面下载 Excel，实际读回 7 张 sheet |
| 识别保存依赖前台工作台且强制 mode local | 保存层接受明确 provider/source；可信目标身份、文档版本/哈希/类别与回滚；前台默认只使用本地引擎 | document-recognition-workflow 与 task 用例通过，覆盖后台 provider 保存、未分配账期不被自动改写、人工字段不改、版本/取消/身份竞态回滚。证据为 Node 测试，未实测远端模型或 OCR 全流程 |

## 最终验证与发布状态

以下由总控执行并回传，实施任务未自行运行测试或构建：

- 最终统一测试 413/413 通过，0 失败，日志为 `/tmp/financedesk-full-chain-review/final-suite-3.tap`。
- `npm run build` 成功，Sites 所需产物及单文件 HTML 已生成；仍有既有的大 chunk 提示。
- 浏览器基于本轮新 dist 完成表内列明的操作；预览 `browser.errors` 无错误。直接通过 `file://` 打开单文件 HTML，主页显示实际内容且无 JavaScript 错误；该证据不代表单文件所有业务流程均已验收。
- 本文记录提交前的验收证据；总控在 commit、push 后核对对应提交的生产部署和线上入口，发布结果另行回传。本地构建和浏览器通过本身不等于已经上线。

## 保留的业务约束与剩余项

- 已入账凭证仍需通过更正，归档历史不直接改写；凭证原件、人工意见、工资 confirm.finance 及客户 confirm.owner 权限保持。
- 普通草稿取消解除自己的待办，不取消真实业务本身必须完成的核对事项。
- 已结算/已入账账单金额差异，跨期调整自动联动仍待后续方案；本轮结束时预收/预付冲销尚不支持直接撤回，后续实现见 [9 月 12 日修复记录](2026-09-12-advance-reversal.md)。现有核销更正只支持同金额换目标，不能替代金额更正。
- 未进行 Windows、iPad 或其他跨设备验收；识别提供方与并发行为由 Node 用例验证，远端模型调用及 OCR 全流程没有实际验收证据。
- 原文件、识别正文、会计资料继续本地保存；外部平台连接、模型调用、联网识别及自动确认均未实施。
